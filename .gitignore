# env
.env
.env.local
.env.*.local

# deps
node_modules/
.pnpm-store/

# build
dist/
.next/
.turbo/
*.tsbuildinfo

# logs
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Claude Code local settings (machine-specific)
.claude/settings.local.json

# test
coverage/
.vercel
.env*

# Local Claude Code MCP server config (personal, not for the repo)
.mcp.json

# Ad-hoc / one-off DB scripts (prod probes, data fixes, audits). These are
# throwaway and must never be committed — they invite accidental re-runs against
# production. Keep them local; promote anything reusable to a named, reviewed
# script instead.
packages/db/_*.mjs
