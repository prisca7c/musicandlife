interface:
  display_name: "Musicandlife"
  short_description: "Repo-specific patterns and workflows for musicandlife"
  default_prompt: "Use the musicandlife repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true